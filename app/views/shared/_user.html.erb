<div class="unit">
  <h2></h2>
  <div class="subunit">
    <table class="user_info">
      <tbody>
        <tr>
          <th>ユーザー名</th>
          <td><%= user.username %></td>
        </tr>
        <% if edit %>
          <tr>
            <th>RP表公開URL</th>
            <td>
              <%= auto_link view_url(sprintf_for_user_id(user.id), only_path: false) %>
              <br/>
              <%= auto_link iglock_url(sprintf_for_user_id(user.id), only_path: false) %>（未取得/ロックフラグ無視版）
            </td>
          </tr>
        <% end %>
        <% if user.cxb_id %>
          <tr>
            <th>C×B ID</th>
            <td><%= user.cxb_id %></td>
          </tr>
        <% end %>
        <% if user.comment %>
          <tr>
            <th>コメント</th>
            <td><%= simple_format auto_link(h(user.comment)) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <% if edit %>
      <p>
        <%= link_to 'ユーザー情報編集', edit_user_url %>
      </p>
    <% else %>
      <p>
        <%= link_to_unless_current '通常RP表', view_url(sprintf_for_user_id(user.id)) %>
        / <%= link_to_unless_current 'ロック状態無視RP表', iglock_url(sprintf_for_user_id(user.id)) %>
      </p>
    <% end %>
  </div>
</div>
