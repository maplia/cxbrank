<% breadcrumb :musics %>
<%= breadcrumbs %>

<h1><%= h1_title(@page_title) %></h1>
<p>間違いがあれば、ご指摘ください。確認の上、修正を行います。</p>

<% @music_set.each do |block| %>
  <%= render 'block', block: block %>
<% end %>
